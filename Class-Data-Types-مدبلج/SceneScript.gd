extends Node

#الفئة كأنواع بيانات
var هدف: Object
var العقدة: Node
var حيوان: Animal

# يتم استدعاؤها عندما تدخل العقدة إلى شجرة المشهد لأول مرة.
func _ready():
	
	هدف = Frog.new() as Object
	العقدة = Frog.new() as Node
	حيوان = Frog.new() as Animal
	
	# يحاول المحرك تحويل أنواع البيانات حتى لو لم يتم كتابتها صراحةً في الكود
	هدف = Frog.new() # لاحظ أن الكلمة الأساسية "as" مفقودة
	
	حيوان.eat() # لاحظ كيف يمكننا استخدام وظيفة الأكل، والصب نجح!
	
